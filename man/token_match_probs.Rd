% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/token_match_probs.R
\name{token_match_probs}
\alias{token_match_probs}
\title{Prepare token match probability lookup tables for use with nmatch_fast_prob}
\usage{
token_match_probs(
  x,
  y,
  nchar_min = 2L,
  std = name_standardize,
  ...,
  batch_size = 1000L,
  verbose = TRUE
)
}
\arguments{
\item{x}{Character vector of names from the x-side corpus}

\item{y}{Character vector of names from the y-side corpus}

\item{nchar_min}{Minimum token size to compare. Defaults to \code{2L}.}

\item{std}{Function to standardize strings during matching. Defaults to
\code{\link{name_standardize}}. Set to \code{NULL} to omit standardization.}

\item{...}{additional arguments passed to \code{std()}}

\item{batch_size}{Number of unique x-side tokens to process per batch.
Reducing this lowers peak memory at the cost of slightly more overhead.
Defaults to \code{1000L}.}

\item{verbose}{If \code{TRUE} (default), prints progress after each batch.}
}
\value{
A named list with two data frames:
\itemize{
\item \code{x}: columns \code{token} (character), \code{dist} (integer), \code{prob} (numeric) for
the x-side corpus
\item \code{y}: columns \code{token} (character), \code{dist} (integer), \code{prob} (numeric) for
the y-side corpus
}

Each row gives the cumulative probability \verb{P(observed distance <= dist | token)}. Pass these to \code{\link{nmatch_fast_prob}} as:
\preformatted{
probs <- token_match_probs(corpus_x, corpus_y)
nmatch_fast_prob(
  x, y,
  token_x = probs$x$token, dist_x = probs$x$dist, prob_x = probs$x$prob,
  token_y = probs$y$token, dist_y = probs$y$dist, prob_y = probs$y$prob
)
}
}
\description{
Computes per-token cumulative false-positive match probabilities by
cross-joining all token pairs from two name corpora, computing their OSA
string distances, and deriving empirical cumulative distributions. The
returned tables can be passed directly to \code{\link{nmatch_fast_prob}}.
}
\details{
For each token \code{t} in a corpus, the cumulative probability at distance \code{d}
is \verb{P(dist(t, t') <= d)}, where \verb{t'} is a token drawn at random from the
opposite corpus (weighted by token frequency). This represents the
false-positive rate: how likely is it to observe a match this good by
chance?
}
