% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/token_idf.R
\name{token_idf}
\alias{token_idf}
\title{Prepare token IDF tables for use with nmatch_fast_prob}
\usage{
token_idf(x, y, nchar_min = 2L, std = name_standardize, ...)
}
\arguments{
\item{x}{Character vector of names from the x-side corpus}

\item{y}{Character vector of names from the y-side corpus}

\item{nchar_min}{Minimum token size to compare. Defaults to \code{2L}.}

\item{std}{Function to standardize strings during matching. Defaults to
\code{\link{name_standardize}}. Set to \code{NULL} to omit standardization.}

\item{...}{additional arguments passed to \code{std()}}
}
\value{
A named list with two data frames:
\itemize{
\item \code{x}: columns \code{token} (character) and \code{idf} (numeric) for the x-side corpus
\item \code{y}: columns \code{token} (character) and \code{idf} (numeric) for the y-side corpus
}

Pass these to \code{\link{nmatch_fast_prob}} as:
\preformatted{
idfs <- token_idf(corpus_x, corpus_y)
nmatch_fast_prob(
  x, y,
  token_idf_x = idfs$x,
  token_idf_y = idfs$y
)
}
}
\description{
Computes per-token inverse document frequency (IDF) weights from two name
corpora. The returned tables can be passed directly to
\code{\link{nmatch_fast_prob}} as \code{token_idf_x} and
\code{token_idf_y}.
}
\details{
For each token \code{t} in a corpus of \code{N} names, IDF is computed as:
\deqn{IDF(t) = \log(N / df(t))}
where \code{df(t)} is the number of names containing token \code{t} at
least once. Tokens that appear in every name receive IDF = 0; rare tokens
receive higher values.
}
